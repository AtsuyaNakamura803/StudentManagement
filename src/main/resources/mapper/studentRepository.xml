<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="raisetech.Student.Management.repository.StudentRepository">

    <select id="findAll" resultType="raisetech.Student.Management.data.Student">
        SELECT id, name, age, is_deleted
        FROM students
        WHERE is_deleted = FALSE
    </select>

    <select id="findById" parameterType="java.lang.Long" resultType="raisetech.Student.Management.data.Student">
        SELECT id, name, age, is_deleted
        FROM students
        WHERE id = #{id} AND is_deleted = FALSE
    </select>

    <select id="findCoursesByStudentId" parameterType="java.lang.Long" resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT id, student_id, course_name, is_deleted
        FROM students_courses
        WHERE student_id = #{studentId} AND is_deleted = FALSE
    </select>

    <insert id="insertStudent" parameterType="raisetech.Student.Management.data.Student" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO students (name, age, is_deleted)
        VALUES (#{name}, #{age}, FALSE)
    </insert>

    <update id="updateStudent" parameterType="raisetech.Student.Management.data.Student">
        UPDATE students
        SET name = #{name}, age = #{age}
        WHERE id = #{id} AND is_deleted = FALSE
    </update>

    <insert id="insertStudentCourse" parameterType="raisetech.Student.Management.data.StudentCourse" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO students_courses (student_id, course_name, is_deleted)
        VALUES (#{studentId}, #{courseName}, FALSE)
    </insert>

    <update id="deleteStudentCourses" parameterType="java.lang.Long">
        UPDATE students_courses
        SET is_deleted = TRUE
        WHERE student_id = #{id}
    </update>

    <update id="deleteStudent" parameterType="java.lang.Long">
        UPDATE students
        SET is_deleted = TRUE
        WHERE id = #{id}
    </update>

</mapper>