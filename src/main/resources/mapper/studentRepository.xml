<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="raisetech.Student.Management.repository.StudentRepository">

  <!-- 全学生取得 -->
  <select id="findAll" resultType="raisetech.Student.Management.data.Student">
    SELECT id, name, email, age, sex, deleted
    FROM students
    WHERE deleted = 0
  </select>

  <!-- 指定IDの学生取得 -->
  <select id="findById" parameterType="long" resultType="raisetech.Student.Management.data.Student">
    SELECT id, name, email, age, sex, deleted
    FROM students
    WHERE id = #{id} AND deleted = 0
  </select>

  <!-- 学生登録 -->
  <insert id="insertStudent" parameterType="raisetech.Student.Management.data.Student" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students (name, email, age, sex, deleted)
    VALUES (#{name}, #{email}, #{age}, #{sex}, #{deleted})
  </insert>

  <!-- 学生更新 -->
  <update id="updateStudent" parameterType="raisetech.Student.Management.data.Student">
    UPDATE students
    SET name = #{name},
    email = #{email},
    age = #{age},
    sex = #{sex}
    WHERE id = #{id} AND deleted = 0
  </update>

  <!-- 学生論理削除 -->
  <update id="deleteStudent" parameterType="long">
    UPDATE students
    SET deleted = 1
    WHERE id = #{id}
  </update>

</mapper>