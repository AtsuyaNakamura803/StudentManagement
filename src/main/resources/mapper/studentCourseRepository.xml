<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="raisetech.Student.Management.repository.StudentCourseRepository">

    <!-- 全件取得 -->
    <select id="findAll" resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT id, student_id, course_name, course_start_at, course_end_at, deleted
        FROM students_courses
        WHERE deleted = false
    </select>

    <!-- 学生IDでコース一覧を取得 -->
    <select id="findByStudentId" parameterType="long"
            resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT id, student_id, course_name, course_start_at, course_end_at, deleted
        FROM students_courses
        WHERE student_id = #{studentId} AND deleted = false
    </select>

    <!-- コース一括追加 -->
    <insert id="insertAll" parameterType="java.util.List">
        INSERT INTO students_courses (student_id, course_name, course_start_at, course_end_at, deleted)
        VALUES
        <foreach collection="list" item="course" separator=",">
            (#{course.studentId}, #{course.courseName}, #{course.courseStartAt}, #{course.courseEndAt}, false)
        </foreach>
    </insert>

    <!-- コース一括更新 -->
    <update id="updateAll" parameterType="java.util.List">
        <foreach collection="list" item="course" separator=";">
            UPDATE students_courses
            SET course_name = #{course.courseName},
            course_start_at = #{course.courseStartAt},
            course_end_at = #{course.courseEndAt},
            deleted = #{course.deleted}
            WHERE id = #{course.id}
        </foreach>
    </update>

    <!-- 学生IDでコース削除（論理削除） -->
    <update id="deleteByStudentId" parameterType="long">
        UPDATE students_courses
        SET deleted = true
        WHERE student_id = #{studentId}
    </update>

</mapper>