<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="raisetech.Student.Management.repository.StudentCourseRepository">

    <!-- 学生IDでコース取得 -->
    <select id="findByStudentId" parameterType="long" resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT id, student_id, course_name, course_start_at, course_end_at, deleted
        FROM students_courses
        WHERE student_id = #{studentId} AND deleted = 0
    </select>

    <!-- 全コース取得 -->
    <select id="findAll" resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT id, student_id, course_name, course_start_at, course_end_at, deleted
        FROM students_courses
        WHERE deleted = 0
    </select>

    <!-- コース一括登録 -->
    <insert id="insertAll" parameterType="list">
        <foreach collection="courses" item="course" separator=";">
            INSERT INTO students_courses (student_id, course_name, course_start_at, course_end_at, deleted)
            VALUES (#{course.studentId}, #{course.courseName}, #{course.courseStartAt}, #{course.courseEndAt}, 0)
        </foreach>
    </insert>

    <!-- コース一括更新 -->
    <update id="updateAll" parameterType="list">
        <foreach collection="courses" item="course" separator=";">
            UPDATE students_courses
            SET course_name = #{course.courseName},
            course_start_at = #{course.courseStartAt},
            course_end_at = #{course.courseEndAt}
            WHERE id = #{course.id} AND student_id = #{course.studentId}
        </foreach>
    </update>

    <!-- 学生IDでコース論理削除 -->
    <update id="deleteByStudentId" parameterType="long">
        UPDATE students_courses
        SET deleted = 1
        WHERE student_id = #{studentId}
    </update>

</mapper>