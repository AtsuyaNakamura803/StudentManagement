<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="raisetech.Student.Management.repository.StudentCourseRepository">

    <!-- 学生IDでコース取得 -->
    <select id="findByStudentId" resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT *
        FROM student_courses
        WHERE student_id = #{studentId}
        ORDER BY id ASC
    </select>

    <!-- 全コース取得 -->
    <select id="findAll" resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT *
        FROM student_courses
        ORDER BY id ASC
    </select>

    <!-- コース一括登録 -->
    <insert id="insertAll">
        INSERT INTO student_courses (student_id, course_name, created_at, updated_at)
        VALUES
        <foreach collection="list" item="course" separator=",">
            (#{course.studentId}, #{course.courseName}, NOW(), NOW())
        </foreach>
    </insert>

    <!-- コース更新 -->
    <update id="update">
        UPDATE student_courses
        SET course_name = #{courseName}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 学生IDでコース削除 -->
    <delete id="deleteByStudentId">
        DELETE FROM student_courses
        WHERE student_id = #{studentId}
    </delete>

</mapper>