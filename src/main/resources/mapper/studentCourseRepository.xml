<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="raisetech.Student.Management.repository.StudentCourseRepository">

    <!-- 複数コース一括挿入 -->
    <insert id="insertAll" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO students_courses
        (student_id, course_name, course_start_at, course_end_at, deleted)
        VALUES
        <foreach collection="list" item="course" separator=",">
            (#{course.studentId}, #{course.courseName}, #{course.courseStartAt}, #{course.courseEndAt}, 0)
        </foreach>
    </insert>

    <!-- 単一コース更新 -->
    <update id="update" parameterType="raisetech.Student.Management.data.StudentCourse">
        UPDATE students_courses
        SET
        course_name = #{courseName},
        course_start_at = #{courseStartAt},
        course_end_at = #{courseEndAt}
        WHERE id = #{id}
    </update>

    <!-- 学生IDで取得 -->
    <select id="findByStudentId" parameterType="java.lang.Long" resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT id, student_id, course_name, course_start_at, course_end_at, deleted
        FROM students_courses
        WHERE student_id = #{id} AND deleted = 0
    </select>

    <!-- 全コース取得（論理削除除外） -->
    <select id="findAll" resultType="raisetech.Student.Management.data.StudentCourse">
        SELECT id, student_id, course_name, course_start_at, course_end_at, deleted
        FROM students_courses
        WHERE deleted = 0
    </select>

    <!-- 学生IDで論理削除 -->
    <update id="deleteByStudentId" parameterType="java.lang.Long">
        UPDATE students_courses
        SET deleted = 1
        WHERE student_id = #{id}
    </update>

</mapper>